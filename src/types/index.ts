export type ProductType = {
  image: string;
  name: string;
  description: string;
  price: number;
  currency: string;
};

export type CompanyType = {
  website: string;
  name: string;
  image: string;
};
